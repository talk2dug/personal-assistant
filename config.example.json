{
  "telegram_bot_token": "PUT_YOUR_BOTFATHER_TOKEN_HERE",
  "ollama_host": "http://192.168.0.139:11434",
  "ollama_model": "gemma4:12b-it-q4_K_M",
  "_comment_local_llm": "The local-first fast path (assistant/core/local_fast_path.py): tries a local Ollama model before the cloud backend for simple, well-scoped Home Assistant commands, since HA's own calls are consistently sub-second and the real latency is model round-trip time. Deliberately a separate host/model from ollama_host/ollama_model above (which only matters when llm_backend=='ollama', and is shared with the GPU bridge) -- point this at dedicated local-LLM hardware once it exists. Leave local_llm_host unset to disable the fast path entirely; every request then behaves exactly as it does without it.",
  "local_llm_host": null,
  "local_llm_model": "gemma4:12b-it-q4_K_M",
  "local_llm_timeout_seconds": 20.0,
  "local_llm_keepalive_interval_seconds": 600,
  "db_path": "jarvis.db",
  "poll_interval_seconds": 30,
  "timezone": "America/New_York",
  "era_api_key": "PUT_YOUR_ERA_API_KEY_HERE",
  "era_mcp_url": "https://context.era.app/mcp",
  "era_sensitive_tools": [
    "billing__upgrade",
    "billing__cancel_subscription",
    "billing__uncancel_subscription",
    "connections__disconnect_institution",
    "accounts__manage_account",
    "knowledge__reset_pack_questions"
  ],
  "recipe_api_key": "PUT_YOUR_RECIPE_API_KEY_HERE",
  "recipe_mcp_url": "https://recipe-api.com/api/mcp",
  "apple_id": "PUT_YOUR_APPLE_ID_EMAIL_HERE",
  "apple_app_password": "PUT_YOUR_APP_SPECIFIC_PASSWORD_HERE",
  "caldav_url": "https://caldav.icloud.com",
  "caldav_personal_calendar": "PUT_PERSONAL_CALENDAR_URL_HERE",
  "caldav_shared_calendar": "PUT_SHARED_CALENDAR_URL_HERE",
  "caldav_sync_interval_seconds": 300,
  "era_cache_interval_seconds": 3600,
  "web_session_secret": "GENERATE_A_RANDOM_SECRET_HERE",
  "web_port": 8080,
  "phone_mcp_url": "http://PUT_YOUR_PHONE_LAN_IP_HERE:3000/mcp",
  "phone_sensitive_tools": ["send_sms", "make_call", "shell"],
  "stt_model_size": "small.en",
  "mail_sensitive_tools": ["send_email"],
  "_comment_mail_junk": "Autonomous junk-flagging (stage 1 of email triage): every mail_junk_scan_interval_seconds, unread INBOX mail is scored by assistant/core/junk_filter.py and anything scoring at or above mail_junk_score_threshold is moved to the Junk folder. Not gated behind a confirmation -- it's reversible (moved, not deleted) and the whole point is that it runs unattended.",
  "mail_junk_scan_interval_seconds": 900,
  "mail_junk_score_threshold": 4.0,
  "_comment_kroger_sync": "Every kroger_sync_interval_seconds, kitchen_db.sync_kroger_orders checks for a newly-placed Kroger order and folds its items into kitchen inventory. Only ever finds something if the cart was built through Jarvis (chat or add_recipe_to_cart) and then checked out AND marked placed -- Kroger's API has no permission to report a trip made independently on Kroger's own app/site, so this covers a narrow case, not everyday Kroger shopping (use record_purchase or a receipt scan for that).",
  "kroger_sync_interval_seconds": 3600,
  "_comment_watchdog": "The watchdog (scheduler.py's run_task_watchdog/run_review_watchdog/run_github_watchdog, see docs/watchdog-system-design.md): every task_watchdog_interval_seconds, a personal task whose due_at has arrived gets notified directly (mechanical, no LLM call). Every review_watchdog_interval_seconds, any pending Review item older than review_watchdog_stale_hours -- including a sensitive-tool confirmation like a PR ready to merge -- gets a short handle_message-generated nudge. Every github_watchdog_interval_seconds, every open PR is polled via the same GitOpsClient every git tool uses, and any real change (a check just failed, a PR became (un)mergeable, a merge/close) gets nudged the same way -- this is what catches a dev-team job stalling or failing without the owner having to ask.",
  "task_watchdog_interval_seconds": 60,
  "review_watchdog_interval_seconds": 900,
  "review_watchdog_stale_hours": 2.0,
  "github_watchdog_interval_seconds": 180,
  "obsidian_vault_path": "C:\\PUT_YOUR_VAULT_PATH_HERE",
  "ha_base_url": "http://PUT_YOUR_HOME_ASSISTANT_IP_HERE",
  "ha_token": "PUT_YOUR_HA_LONG_LIVED_ACCESS_TOKEN_HERE",
  "ha_sensitive_domains": ["lock", "cover", "alarm_control_panel"],
  "ha_conversation_api_key": "GENERATE_A_RANDOM_SECRET_HERE",
  "_comment_llm_backend": "'ollama' runs on the local model; 'claude_cli' runs on the Claude Code CLI (and so the user's claude.ai subscription). Switching is this one line.",
  "llm_backend": "claude_cli",
  "claude_cli_path": "C:\\Users\\YOU\\.local\\bin\\claude.exe",
  "claude_model": "sonnet",
  "claude_timeout_seconds": 300,
  "_comment_claude_tools_api_key": "Authenticates the MCP bridge subprocess to /api/tools/call. Equivalent to full owner access - anything holding it can invoke every Jarvis tool.",
  "claude_tools_api_key": "GENERATE_A_RANDOM_SECRET_HERE",
  "claude_tools_url": "http://127.0.0.1:8080/api/tools/call",
  "_comment_business": "Omit this whole block and the business/second-in-command side stays off — no tools, no agents, no digest. Nothing here belongs in code: the predecessor project hardcoded its city and the module died when the business moved.",
  "business": {
    "name": "YOUR BUSINESS NAME",
    "location": "City, ST",
    "radius_miles": 60,
    "product_lines": ["What you sell", "And what else"],
    "notes": "Any standing context the research agents should know.",
    "min_fit_score": 40
  },
  "_comment_gpu_bridge": "simrig as a shared inference resource for agent work that doesn't need Claude (image analysis, bulk text). Queued, model-routed, and released to the owner the moment he says he's gaming or racing. gpu_task_models overrides the task->model routing; put model choices here, not in code, because they age in weeks.",
  "gpu_bridge_enabled": true,
  "gpu_bridge_host": "http://192.168.0.139:11434",
  "gpu_max_concurrent": 2,
  "gpu_task_models": null,
  "_comment_comfy": "ComfyUI on the GPU box for image (Z-Image Turbo) and video (Wan 2.2 TI2V-5B) generation. A separate server from Ollama on the same machine. Omit comfy_host and image/video jobs fail with a clear message instead of running.",
  "comfy_host": "http://192.168.0.139:8188",
  "generated_media_path": "D:\\Jarvis Generated",
  "_comment_business_agents": "Master switch for unattended agent runs. Off means nothing fires on a timer; agents still run on demand from chat.",
  "business_agents_enabled": false,
  "market_scan_interval_hours": 72,
  "trend_scan_interval_hours": 24,
  "research_queue_interval_minutes": 120,
  "pipeline_interval_hours": 12,
  "business_digest_hour": 8,
  "_comment_personal_research": "How often the background agent checks for personal errands ('find me a dentist') to research. Not gated behind business_agents_enabled — this runs regardless.",
  "personal_research_interval_minutes": 30,
  "_comment_vision": "Camera-based presence and identity: YOLO11n person/pet detection (assistant/core/detector.py, already used) plus InsightFace face matching (assistant/core/face_id.py, new). Off by default -- flip vision_enabled once cameras are registered (via /api/vision/cameras or chat) and .venv-vision has requirements-vision.txt installed on the machine with the RTX 3060. The detection loop only ever runs in the core process (main.py), never the web UI process. vision_face_match_threshold genuinely needs calibrating against real cameras/faces before trusting it -- 0.38 is a starting point, not a promise.",
  "vision_enabled": false,
  "vision_device": null,
  "vision_model_name": "yolo11n.pt",
  "vision_face_model_name": "buffalo_l",
  "vision_poll_seconds": 2.0,
  "vision_face_match_threshold": 0.38,
  "vision_unknown_face_ask_after": 3,
  "vision_presence_window_seconds": 180,
  "_comment_github": "Dev-team agents: git branch/push/PR tools and the ops-plan/SSH workflow. Use a fine-grained PAT scoped to just this one repo (Contents + Pull requests, read/write) — not a classic token with broader access.",
  "github_repo": "REPLACE_WITH_YOUR_GITHUB_OWNER/REPLACE_WITH_YOUR_REPO_NAME",
  "github_pat": "PUT_YOUR_GITHUB_FINE_GRAINED_PAT_HERE",
  "git_workspace_path": "../jarvis-git-workspace",
  "git_author_name": "Jarvis",
  "git_author_email": "jarvis@localhost",
  "_comment_ssh_hosts": "Named hosts the ops-plan workflow may target — short name to {host, user, key_path or password}. The model only ever refers to a host by its short name; real address/credential stay server-side.",
  "ssh_hosts": {
    "example-host": {"host": "192.168.1.50", "user": "pi", "key_path": "~/.ssh/id_ed25519"}
  },
  "users": [
    {"telegram_chat_id": "REPLACE_WITH_YOUR_TELEGRAM_CHAT_ID", "display_name": "Dug", "role": "owner", "web_password": "PICK_A_PASSWORD"},
    {"telegram_chat_id": "REPLACE_WITH_GF_TELEGRAM_CHAT_ID", "display_name": "GF", "role": "partner", "web_password": "PICK_A_PASSWORD"}
  ]
}
