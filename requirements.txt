ollama>=0.4.0
python-telegram-bot>=21.0
APScheduler>=3.10
pytest>=8.0
mcp>=1.0.0
caldav>=1.3.0
fastapi>=0.115.0
uvicorn>=0.30.0
itsdangerous>=2.2.0
python-multipart>=0.0.9
faster-whisper>=1.0.0
# Used directly by home_assistant_client and the Claude CLI backend's MCP bridge. It
# arrives transitively via mcp/fastapi anyway, but the bridge runs as its own subprocess
# and shouldn't depend on another package's dependency tree to keep working.
httpx>=0.27.0
# Renders LetterStream mail bodies to PDF (assistant/core/letter_pdf.py).
fpdf2>=2.8.0
# core/vision.py's motion gate and embedding math (cosine similarity, L2 normalisation).
# Deliberately just numpy here, not the vision stack's heavy CUDA dependencies -- those
# (torch/ultralytics/insightface) stay isolated in requirements-vision.txt and only ever
# load inside scripts/vision_worker.py's own .venv-vision. See core/detector.py's
# docstring for why that split exists.
numpy>=1.26.0
# SSH for the dev-team ops-plan workflow (assistant/core/ssh_ops.py). Was previously used
# by device/deploy_to_pi.py and scripts/scan_network_media.py without ever being declared
# here -- both only worked because it happened to already be installed by hand.
paramiko>=3.4.0
