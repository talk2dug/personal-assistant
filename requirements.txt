ollama>=0.4.0
python-telegram-bot>=21.0
APScheduler>=3.10
pytest>=8.0
mcp>=1.0.0
caldav>=1.3.0
fastapi>=0.115.0
uvicorn>=0.30.0
itsdangerous>=2.2.0
python-multipart>=0.0.9
faster-whisper>=1.0.0
# Used directly by home_assistant_client and the Claude CLI backend's MCP bridge. It
# arrives transitively via mcp/fastapi anyway, but the bridge runs as its own subprocess
# and shouldn't depend on another package's dependency tree to keep working.
httpx>=0.27.0
# Renders LetterStream mail bodies to PDF (assistant/core/letter_pdf.py).
fpdf2>=2.8.0
# core/vision.py's motion gate and embedding math (cosine similarity, L2 normalisation).
# Deliberately just numpy here, not the vision stack's heavy CUDA dependencies -- those
# (torch/ultralytics/insightface) stay isolated in requirements-vision.txt and only ever
# load inside scripts/vision_worker.py's own .venv-vision. See core/detector.py's
# docstring for why that split exists.
numpy>=1.26.0
# SSH for the dev-team ops-plan workflow (assistant/core/ssh_ops.py). Was previously used
# by device/deploy_to_pi.py and scripts/scan_network_media.py without ever being declared
# here -- both only worked because it happened to already be installed by hand.
paramiko>=3.4.0
# --- vinyl cutter (assistant/core/vinyl_cutter.py) ---------------------------------------
# All four of these are declared here on purpose. The print-station system this replaces died
# partly because `paper` and `potrace` were installed by hand on the server and never written
# down, so when the box went, nobody could rebuild it.
#
# Polygon offsetting for cut paths. Not optional and not substitutable: Clipper removes
# self-intersections as part of the offset algorithm, which is precisely what the old
# naive-normal-displacement code got wrong (bowties at every concave corner).
pyclipper>=1.3.0
# Full-resolution contour tracing (findContours with RETR_CCOMP, for holes). The -headless
# build, matching requirements-vision.txt's choice: same API, no GUI libraries, and the
# services this runs in have no display. ~44 MB wheel, prebuilt for Windows, no build step.
# potrace was the alternative and was rejected: its Python bindings need a C toolchain on
# Windows, which is the kind of awkward install that killed the last system.
opencv-python-headless>=4.10.0
# Compositing the printed sheet and writing the PNG at a real DPI.
# Also fixes a pre-existing gap: vision.py and vision_runtime.py already fall back to PIL
# when cv2 is unavailable, but nothing declared it, so that fallback was relying on Pillow
# happening to be installed.
Pillow>=10.0.0
# USB transport for the vendored Silhouette Cameo driver (vendor/inkscape-silhouette).
# That driver shipped its own copy of pyusb 1.0.2 as a *fallback* (Graphtec.py appends it to
# sys.path, so an installed pyusb already won); we dropped the vendored copy and declare it
# here instead. Importing nothing at module scope in Jarvis -- only vinyl_cutter's optional
# hardware path needs it, and file generation works fine without a cutter attached.
pyusb>=1.2.1
pypdf
